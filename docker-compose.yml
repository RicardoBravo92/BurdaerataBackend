services:
  backend:
    build: .
    container_name: burdaerata_backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: always
    command: >
      sh -c "uv run uvicorn app.main:app --host 0.0.0.0 --port 8000"